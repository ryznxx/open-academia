import type { Config } from "$lib/types/config.types";

export const config: Config = {
  appName: "Open Academia",
  appLogo: "https://sit.poliwangi.ac.id/images/navbar-brand.svg",
  signinWelcomingMessage: "The simples way to manage academic"
}